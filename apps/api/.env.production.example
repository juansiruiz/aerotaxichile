# ─── API — .env de producción (modo PM2 sin Docker) ────────────────────────
# Copiar a apps/api/.env.production

DATABASE_URL=postgresql://aerotaxi:PASSWORD@localhost:5432/aerotaxichile
JWT_SECRET=GENERAR_64_CHARS_ALEATORIOS
PORT=4000
ALLOWED_ORIGINS=https://aerotaxichile.cl,https://www.aerotaxichile.cl
API_BASE_URL=https://aerotaxichile.cl/api

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@aerotaxichile.cl
