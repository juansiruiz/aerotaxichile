# ─── Web — .env de producción ──────────────────────────────────────────────
# Copiar a apps/web/.env.production
# Estas variables se inyectan en build time, así que hay que rebuild si cambian.

NEXT_PUBLIC_API_URL=https://aerotaxichile.cl/api
NEXT_PUBLIC_WHATSAPP_NUMBER=56963552132
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
